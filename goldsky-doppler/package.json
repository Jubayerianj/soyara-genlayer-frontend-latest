{
  "name": "doppler-goldsky-subgraph",
  "version": "1.0.0",
  "description": "Goldsky Subgraph for Doppler Launchpad & DEX on LitVM",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy doppler-litvm/v1.0.0 --path ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
